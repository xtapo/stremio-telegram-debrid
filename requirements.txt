fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pyrofork>=2.3.0
tgcrypto>=1.2.5
python-dotenv>=1.0.0
httpx>=0.24.0
h2>=4.1.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
markupsafe>=2.1.0
edge-tts>=6.1.3
pillow>=10.0.0

# Optional extra, only for the MoviesDrive anti-bot fallback
# (MD_ANTIBOT_FALLBACK=true + MD_IMPERSONATE). The addon works without it and
# simply skips the TLS-impersonation attempt when the import fails.
# curl_cffi>=0.7.0
